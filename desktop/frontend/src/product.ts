// Canonical product identity for code-owned labels and process metadata.
// Localized prose remains in the locale catalogs; stable plugin/service IDs
// remain declared by the owner that publishes each identifier.
export const PRODUCT_SLUG = "flame" as const;
export const PRODUCT_NAME = "Flame" as const;
